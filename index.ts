// Minimal stub — bun-unpublished-version-probe
// Pattern: E1 (unpublished-version)
// request@2.88.2 is deprecated; the entire `request` package family was deprecated in 2020.
// This file exists only to make the project a valid TypeScript module.
// It is NOT meant to be executed.

// The import below would work at runtime (the package is still resolvable from the registry
// despite being deprecated), but this project is a Mend detection probe — not an application.
// Do NOT run `bun install` or execute this file.

export {};